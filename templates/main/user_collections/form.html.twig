{% extends 'base.html.twig' %}
{% block body %}
    {{ form_start(collectionForm) }}
    <div class="form-login">
        <div class="col-12">
            <h1>
                {{ title }}
            </h1>
            <div class="form-group">
                <div class="col-sm-12">
                    {{ form_widget(collectionForm.title, {'attr': {'class': 'form-control','placeholder ':'Enter tag'} }) }}
                </div>
                <div class="col-sm-12">
                    {{ form_widget(collectionForm.description, {'attr': {'class': 'form-control','placeholder ':'Enter tag'} }) }}
                </div>